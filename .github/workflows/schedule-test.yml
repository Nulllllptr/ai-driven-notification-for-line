name: Schedule Test (temporary diagnostic)

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "schedule test fired at $(date -u)"
