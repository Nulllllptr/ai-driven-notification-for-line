<!--
このファイルはテンプレート。新規プロジェクトでは design-doc-guard スキルの
ヒアリング・コード読解を経て、docs/STRUCTURE.md として生成・更新すること。
空欄のまま放置しない。

鳥瞰図設計書は PRODUCT.md/TECH.md/STRUCTURE.md(このファイル)の3ファイルで構成する。
本ファイルは「どう組み立てられているか」だけを扱う。目的はPRODUCT.md、技術面はTECH.mdを参照。
-->

# プロジェクト構造

## 1. ディレクトリ構成
プロジェクトルート直下の主要フォルダと役割を、コメント付きのツリーで示す。
未実装の間は「未実装」と明記する。推測で埋めない。

```
プロジェクトルート/
├── .claude/    # このテンプレート一式
└── docs/       # 設計書一式
```

## 2. 稼働システム一覧
このプロジェクトに含まれる各システムと、対応するシステム単位設計書へのリンク。
番号は100刻み(100, 200, 300…)で新規システムに割り当てる。採番の詳細は
`docs/システムフォルダ/README.md`(採番台帳)を参照。

| 番号 | システム名 | 役割(1行) | システム単位設計書 |
|---|---|---|---|
| 100 | | | docs/システムフォルダ/100-システム仕様書.md |

## 3. 大まかな入力から出力までの流れ
システム間をまたぐデータ・処理の流れを、どこから入り(入力元)、どのシステムを経由し、
どこに出る(出力先)かの単位で記す。可能であれば構成図(HTML/Mermaid)へのパスをここにリンクする。
未実装の間は「未実装」と明記する。
