{
  "_comment": "プロジェクト導入時に logging-config.json としてコピーし、値を編集して使う。このファイル自体は編集しない。",
  "default_level": "INFO",
  "retention_days": null,
  "mask_fields": ["password", "token", "authorization", "email", "credit_card"]
}
